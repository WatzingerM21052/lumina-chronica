# scripts/

Reserved for developer/deployment helper scripts (e.g. a local bootstrap script, a combined "run frontend + backend" dev script, migration helpers) once there's a real repetitive task worth scripting.

Empty for now — Phase 0/1 setup steps are simple enough to run directly via `dotnet`/`npm`/`wrangler`/`gh`. Populated as soon as a concrete workflow needs automating.
