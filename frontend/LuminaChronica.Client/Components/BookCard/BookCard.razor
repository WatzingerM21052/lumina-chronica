@namespace LuminaChronica.Client.Components
@implements IDisposable
@inject ApiClient ApiClient
@inject BlobUrlService BlobUrlService

<a class="book-card book-card-@Size.ToString().ToLowerInvariant()" href="@($"library/books/{Book.Id}")">
    <div class="book-card-cover">
        @if (_coverObjectUrl is not null)
        {
            <img src="@_coverObjectUrl" alt="@Book.Title" />
        }
        else
        {
            <div class="book-card-cover-placeholder" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4">
                    <path d="M4 5.5c2.2-1 5-1 8 0v13c-3-1-5.8-1-8 0z" />
                    <path d="M20 5.5c-2.2-1-5-1-8 0v13c3-1 5.8-1 8 0z" />
                </svg>
            </div>
        }
        <button type="button" class="book-card-favorite @(_isFavorite ? "is-favorite" : "")"
                title="@(_isFavorite ? "Aus Favoriten entfernen" : "Zu Favoriten hinzufügen")"
                @onclick="ToggleFavoriteAsync" @onclick:stopPropagation="true" @onclick:preventDefault="true">
            @(_isFavorite ? "★" : "☆")
        </button>
    </div>
    <div class="book-card-info">
        <div class="book-card-title">@Book.Title</div>
        @if (!string.IsNullOrWhiteSpace(Book.Author))
        {
            <div class="book-card-author">@Book.Author</div>
        }
        @if (ProgressPercentage is { } progress)
        {
            <div class="book-card-progress">
                <div class="book-card-progress-bar" style="width: @(progress)%"></div>
            </div>
        }
    </div>
</a>
